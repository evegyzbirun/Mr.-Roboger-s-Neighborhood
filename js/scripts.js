function numbers(inputMain) {
  const arrayNumbers = [];
  for (let index = 0; index <= inputMain; index += 1) {
    console.log(index);
    arrayNumbers.push(index);
  }
  return numbers;
}

function replace(input) {
  let numbers = replace(input);
  for (let i = 0; i <= numbers; i += 1) {
    if (numbers[i]) {

    }
  }
}


